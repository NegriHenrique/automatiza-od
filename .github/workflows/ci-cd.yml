name: 🚀 CI/CD Pipeline - Ordem do Dia

on:
  push:
    branches: [main, develop]
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".github/**"
      - "!.github/workflows/**"
  pull_request:
    branches: [main]
  release:
    types: [published]

env:
  PYTHON_VERSION: "3.11"

jobs:
  # 🧪 TESTES E QUALIDADE
  tests:
    name: 🧪 Testes e Qualidade de Código
    runs-on: windows-latest

    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      - name: 📦 Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Instalar versões específicas para garantir compatibilidade
          pip install pytest pytest-cov black flake8 mypy bandit
          pip install "typer<0.9.0" "rich<13.0.0" "safety==2.3.5"

      - name: 🎨 Verificar formatação (Black)
        run: |
          black --check --diff .

      - name: 🔍 Linting (Flake8)
        run: |
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=88 --statistics

      - name: 🔒 Verificação de segurança (Bandit)
        run: |
          # Executar análise com parâmetros de linha de comando (mais confiável)
          echo "🔍 Executando análise de segurança com Bandit..."
          bandit -r . --exclude=.venv,tests,build_temp,htmlcov,distribuicao,distribuicao_completa --skip=B101,B601,B603,B607 -f json -o bandit-report.json || true

          # Executar verificação focada em problemas críticos
          echo "🎯 Verificando apenas problemas críticos (Medium/High)..."
          bandit -r . --exclude="*/.venv/*,*/tests/*,*/build_temp/*,*/htmlcov/*,*/distribuicao/*,*/distribuicao_completa/*" --skip=B101,B601,B603,B607 --severity-level medium --confidence-level medium

          # Processar e mostrar resultados usando script Python
          python security_check.py || echo "⚠️ Script de verificação não disponível - usando validação básica"

      - name: 🛡️ Verificar vulnerabilidades (Safety)
        run: |
          safety check --json --output safety-report.json || true
          safety check

      - name: 🧪 Executar testes unitários
        run: |
          pytest tests/ -v --cov=. --cov-report=html --cov-report=xml --cov-fail-under=70

      - name: 📊 Upload coverage para Codecov
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

      - name: 📋 Artefatos de relatórios
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-reports
          path: |
            htmlcov/
            bandit-report.json
            safety-report.json
            coverage.xml

  # 🔨 BUILD DO EXECUTÁVEL
  build:
    name: 🔨 Build Executável Windows
    runs-on: windows-latest
    needs: tests
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' || github.event_name == 'release'

    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      - name: 📦 Instalar dependências de build
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: 🔨 Gerar executável
        run: |
          python build_exe.py

      - name: 🛡️ Análise de segurança do executável
        run: |
          # Verificar se o executável foi criado
          if (Test-Path "distribuicao_completa/GeradorOD.exe") {
            Write-Host "✅ Executável encontrado"
            
            # Verificar integridade do arquivo
            $hash = Get-FileHash "distribuicao_completa/GeradorOD.exe" -Algorithm SHA256
            Write-Host "🔐 Hash SHA256: $($hash.Hash)"
            
            # Criar arquivo de hash para verificação
            $hash.Hash | Out-File "distribuicao_completa/GeradorOD.exe.sha256"
            
            # Verificar tamanho (executáveis muito pequenos ou grandes podem ser suspeitos)
            $size = (Get-Item "distribuicao_completa/GeradorOD.exe").Length / 1MB
            Write-Host "📊 Tamanho: $([math]::Round($size, 2)) MB"
            
            if ($size -lt 10 -or $size -gt 200) {
              Write-Host "⚠️ Tamanho incomum detectado - revisar se necessário"
            }
            
            # Verificar se não é um arquivo de texto (indicaria falha no build)
            $fileType = file "distribuicao_completa/GeradorOD.exe" 2>$null
            if ($fileType -match "text") {
              Write-Error "❌ Executável parece ser arquivo de texto - build falhou"
              exit 1
            }
            
          } else {
            Write-Error "❌ Executável não foi criado!"
            exit 1
          }

      - name: ✅ Verificar executável
        run: |
          if (Test-Path "distribuicao_completa/GeradorOD.exe") {
            Write-Host "✅ Executável criado com sucesso!"
            $size = (Get-Item "distribuicao_completa/GeradorOD.exe").Length / 1MB
            Write-Host "📊 Tamanho: $([math]::Round($size, 2)) MB"
          } else {
            Write-Error "❌ Executável não foi criado!"
            exit 1
          }

      # - name: 🧪 Teste básico do executável
      #   run: |
      #     # Teste se o executável inicia sem erros (modo CLI) com timeout
      #     $exePath = "distribuicao_completa/GeradorOD.exe"
      #     if (Test-Path $exePath) {
      #       Write-Host "🔍 Testando executável com --help..."

      #       $startInfo = New-Object System.Diagnostics.ProcessStartInfo
      #       $startInfo.FileName = $exePath
      #       $startInfo.Arguments = "--help"
      #       $startInfo.UseShellExecute = $false
      #       $startInfo.RedirectStandardOutput = $true
      #       $startInfo.RedirectStandardError = $true

      #       $process = New-Object System.Diagnostics.Process
      #       $process.StartInfo = $startInfo

      #       try {
      #         $process.Start() | Out-Null

      #         # Capturar saída padrão e erro assincronamente
      #         $outputTask = $process.StandardOutput.ReadToEndAsync()
      #         $errorTask = $process.StandardError.ReadToEndAsync()

      #         # Esperar 5 segundos pelo --help
      #         if ($process.WaitForExit(5000)) {
      #           $output = $outputTask.Result
      #           $error = $errorTask.Result

      #           if ($process.ExitCode -eq 0) {
      #             Write-Host "✅ Executável respondeu ao --help com sucesso"
      #             Write-Host "📝 Saída do --help:"
      #             Write-Host $output

      #             # Verificar se a saída contém algo útil
      #             if ($output -match "uso:|usage:|help|ajuda") {
      #               Write-Host "✅ Menu de ajuda detectado!"
      #             } else {
      #               Write-Host "⚠️ Saída do --help não reconhecida, mas executável respondeu"
      #             }
      #           } else {
      #             Write-Host "⚠️ Executável retornou código: $($process.ExitCode)"
      #             Write-Host "📝 Saída de erro:"
      #             Write-Host $error
      #             exit 1
      #           }
      #         } else {
      #           Write-Host "❌ Timeout: --help não respondeu em 5 segundos"
      #           $process.Kill()
      #           exit 1
      #         }
      #       }
      #       catch {
      #         Write-Error "❌ Erro ao executar teste: $_"
      #         exit 1
      #       }
      #       finally {
      #         if (-not $process.HasExited) {
      #           $process.Kill()
      #         }
      #       }
      #     } else {
      #       Write-Error "❌ Executável não encontrado para teste!"
      #       exit 1
      #     }

      - name: 📦 Criar pacote de distribuição
        run: |
          # Executar script de assinatura (sem certificado, apenas validação)
          Write-Host "🔐 Executando validação de segurança do executável..."
          pwsh -ExecutionPolicy Bypass -File "sign_executable.ps1" -ExecutablePath "distribuicao_completa/GeradorOD.exe"

          # Criar ZIP com distribuição completa
          $version = "v2.0-$(Get-Date -Format 'yyyyMMdd')-$($env:GITHUB_SHA.Substring(0,7))"
          Compress-Archive -Path "distribuicao_completa/*" -DestinationPath "OrdemDoDia-$version.zip"

          # Criar hash do arquivo
          $hash = Get-FileHash "OrdemDoDia-$version.zip" -Algorithm SHA256
          $hash.Hash | Out-File "OrdemDoDia-$version.zip.sha256"

          Write-Host "📦 Pacote criado: OrdemDoDia-$version.zip"

          # Criar manifesto de segurança
          $manifest = @{
            version = $version
            build_date = (Get-Date -Format "yyyy-MM-dd HH:mm:ss UTC")
            commit_hash = $env:GITHUB_SHA
            executable_hash = (Get-FileHash "distribuicao_completa/GeradorOD.exe" -Algorithm SHA256).Hash
            package_hash = $hash.Hash
            security_scan = "passed"
            signed = "false"
          }

          $manifest | ConvertTo-Json | Out-File "OrdemDoDia-$version.manifest.json" -Encoding UTF8
          Write-Host "🛡️ Manifesto de segurança criado"

      - name: 📤 Upload artefatos
        uses: actions/upload-artifact@v4
        with:
          name: executavel-windows
          path: |
            OrdemDoDia-*.zip
            OrdemDoDia-*.zip.sha256
            OrdemDoDia-*.manifest.json
          retention-days: 30

  # 🚀 RELEASE AUTOMÁTICO
  release:
    name: 🚀 Create Release
    runs-on: windows-latest
    needs: [tests, build]
    if: github.event_name == 'release' && github.event.action == 'published'

    steps:
      - name: 📥 Download artefatos
        uses: actions/download-artifact@v4
        with:
          name: executavel-windows

      - name: 🚀 Upload para Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            OrdemDoDia-*.zip
            OrdemDoDia-*.zip.sha256
            OrdemDoDia-*.manifest.json
          body: |
            ## 🎉 Nova versão do Ordem do Dia!

            ### 📦 Arquivos de Download
            - **OrdemDoDia-*.zip**: Aplicação completa com executável
            - **OrdemDoDia-*.zip.sha256**: Hash SHA256 para verificação
            - **OrdemDoDia-*.manifest.json**: Manifesto de segurança e integridade

            ### 🚀 Como Usar
            1. Baixe o arquivo ZIP
            2. Extraia em uma pasta de sua escolha
            3. Execute `GeradorOD.exe`
            4. Siga as instruções na interface

            ### 📋 Requisitos
            - Windows 10/11 (64-bit)
            - 4GB RAM mínimo
            - 100MB espaço livre

            ### 🆘 Suporte
            - 📖 Manual: Incluído no ZIP
            - 🐛 Issues: [GitHub Issues](https://github.com/${{ github.repository }}/issues)
            - 📧 Email: [Contato do desenvolvedor]

            ---
            **Testado em**: Windows 10, Windows 11  
            **Build**: ${{ github.sha }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # 📋 NOTIFICAÇÕES E LINKS
  notify:
    name: 📋 Notificações
    runs-on: ubuntu-latest
    needs: [tests, build]
    if: always()

    steps:
      - name: 🔗 Comentário com link do artefato
        if: needs.tests.result == 'success' && needs.build.result == 'success'
        uses: actions/github-script@v7
        with:
          script: |
            const runId = context.runId;
            const repo = context.repo;
            const sha = context.sha;

            const artifactUrl = `https://github.com/${repo.owner}/${repo.repo}/actions/runs/${runId}`;
            const downloadUrl = `${artifactUrl}#artifacts`;

            const comment = `## 🎉 Build Concluído com Sucesso!

            ### 📦 **Download Direto do Executável**
            🔗 **[BAIXAR EXECUTÁVEL AQUI](${downloadUrl})**

            ### 📊 **Resultados do Build**
            - ✅ **Testes**: ${context.payload.workflow_run?.conclusion || 'Passou'} 
            - ✅ **Build**: Sucesso
            - 🏗️ **Commit**: \`${sha.substring(0, 7)}\`
            - 📅 **Data**: ${new Date().toLocaleString('pt-BR')}

            ### 🚀 **Links Rápidos**
            - 📋 [Ver Logs Completos](${artifactUrl})
            - 📦 [Download Artefatos](${downloadUrl})
            - 🔍 [Detalhes do Commit](https://github.com/${repo.owner}/${repo.repo}/commit/${sha})

            ### 💡 **Como Baixar**
            1. Clique no link **"BAIXAR EXECUTÁVEL AQUI"** acima
            2. Role até a seção "Artifacts" 
            3. Clique em **"executavel-windows"**
            4. O download começará automaticamente

            ---
            🤖 *Comentário automático do CI/CD*`;

            // Se for um push, comenta no commit
            if (context.eventName === 'push') {
              await github.rest.repos.createCommitComment({
                owner: repo.owner,
                repo: repo.repo,
                commit_sha: sha,
                body: comment
              });
            }

            // Se for um PR, comenta no PR
            if (context.payload.pull_request) {
              await github.rest.issues.createComment({
                owner: repo.owner,
                repo: repo.repo,
                issue_number: context.payload.pull_request.number,
                body: comment
              });
            }

      - name: 💬 Notificar sucesso
        if: needs.tests.result == 'success' && needs.build.result == 'success'
        run: |
          echo "✅ Pipeline executado com sucesso!"
          echo "🧪 Testes: PASSOU"
          echo "🔨 Build: PASSOU"
          echo "🔗 Link do artefato postado automaticamente!"

      - name: 🚨 Notificar falha
        if: needs.tests.result == 'failure' || needs.build.result == 'failure'
        run: |
          echo "❌ Pipeline falhou!"
          echo "🧪 Testes: ${{ needs.tests.result }}"
          echo "🔨 Build: ${{ needs.build.result }}"
          exit 1
