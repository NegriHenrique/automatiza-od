#!/bin/bash

# 🚀 Pre-push Hook - Ordem do Dia
# Verificações antes do push para garantir qualidade

# Cores para output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Contadores
ERRORS=0
WARNINGS=0

# Função para print com status
print_status() {
    local message="$1"
    local type="$2"
    
    case $type in
        "success") echo -e "${GREEN}✅ $message${NC}" ;;
        "error") echo -e "${RED}❌ $message${NC}" ;;
        "warning") echo -e "${YELLOW}⚠️ $message${NC}" ;;
        "info") echo -e "${BLUE}ℹ️ $message${NC}" ;;
        *) echo "ℹ️ $message" ;;
    esac
}

echo "ℹ️ 🚀 Executando verificações pre-push..."

# Obter informações do branch
BRANCH=$(git rev-parse --abbrev-ref HEAD)
print_status "Branch atual: $BRANCH" "info"

# 1. Executar suite completa de testes
print_status "Executando suite completa de testes..." "info"
if python -m pytest tests/ -v --tb=short; then
    print_status "Todos os testes passaram" "success"
else
    print_status "Testes falharam" "error"
    ERRORS=$((ERRORS + 1))
fi

# 2. Verificar cobertura de testes
print_status "Verificando cobertura de testes..." "info"
COVERAGE=$(python -m pytest --cov=. --cov-report=term-missing tests/ 2>/dev/null | grep "TOTAL" | awk '{print $4}' | sed 's/%//')

if [[ "$COVERAGE" =~ ^[0-9]+$ ]] && [ "$COVERAGE" -ge 70 ]; then
    print_status "Cobertura de testes: ${COVERAGE}%" "success"
else
    print_status "Cobertura de testes insuficiente: ${COVERAGE}% (mínimo: 70%)" "error"
    ERRORS=$((ERRORS + 1))
fi

# 3. Testar geração do executável (crítico)
print_status "Testando geração do executável..." "info"
if python build_exe.py > build_log.txt 2>&1; then
    if [ -f "distribuicao/GeradorOD.exe" ]; then
        print_status "Executável gerado com sucesso" "success"
        # Limpar arquivos temporários de build
        rm -f build_log.txt version_info.txt 2>/dev/null
    else
        print_status "Falha na geração do executável!" "error"
        echo "Últimas linhas do erro:"
        tail -10 build_log.txt
        ERRORS=$((ERRORS + 1))
    fi
else
    print_status "Falha na geração do executável!" "error"
    echo "Últimas linhas do erro:"
    tail -10 build_log.txt
    ERRORS=$((ERRORS + 1))
fi

# 4. Verificar TODOs e FIXMEs
print_status "Verificando TODOs e FIXMEs..." "info"
TODO_COUNT=$(grep -r "TODO\|FIXME" --include="*.py" . 2>/dev/null | wc -l)
if [ "$TODO_COUNT" -eq 0 ]; then
    print_status "Nenhum TODO/FIXME encontrado" "success"
else
    print_status "$TODO_COUNT TODO/FIXME encontrados - considere resolver" "warning"
    WARNINGS=$((WARNINGS + 1))
fi

# 5. Verificar se documentação está atualizada
print_status "Verificando documentação..." "info"
if [ -f "README.md" ]; then
    # Verificar se README foi modificado recentemente
    README_DAYS=$(find README.md -mtime +30 2>/dev/null | wc -l)
    if [ "$README_DAYS" -gt 0 ]; then
        print_status "README pode estar desatualizado" "warning"
        WARNINGS=$((WARNINGS + 1))
    else
        print_status "Documentação OK" "success"
    fi
fi

# 6. Verificações específicas do branch
if [ "$BRANCH" = "main" ]; then
    print_status "Verificações para branch main..." "info"
    
    # Verificar se há muitos commits desde a última tag
    LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null)
    if [ -n "$LAST_TAG" ]; then
        COMMITS_SINCE_TAG=$(git rev-list "$LAST_TAG"..HEAD --count)
        if [ "$COMMITS_SINCE_TAG" -gt 20 ]; then
            print_status "$COMMITS_SINCE_TAG commits desde $LAST_TAG - considere criar nova tag" "warning"
            WARNINGS=$((WARNINGS + 1))
        fi
    fi
fi

# 7. Verificar conflitos de merge
print_status "Verificando conflitos de merge..." "info"
if git diff --check --cached >/dev/null 2>&1; then
    print_status "Nenhum conflito detectado" "success"
else
    print_status "Possíveis conflitos de merge detectados" "error"
    ERRORS=$((ERRORS + 1))
fi

# 8. Verificar qualidade dos commits
print_status "Verificando qualidade dos commits..." "info"
LAST_COMMIT_MSG=$(git log -1 --pretty=%s)
if [[ "$LAST_COMMIT_MSG" =~ ^(feat|fix|docs|style|refactor|test|chore)(\(.+\))?: ]]; then
    print_status "Commit segue Conventional Commits" "success"
else
    print_status "Commit não segue Conventional Commits (recomendado)" "warning"
    WARNINGS=$((WARNINGS + 1))
fi

# Resumo final
echo ""
print_status "📊 Resumo das verificações:" "info"
if [ $ERRORS -eq 0 ]; then
    print_status "✅ Todas as verificações críticas passaram" "success"
else
    print_status "❌ $ERRORS erro(s) encontrado(s). Corrija antes de fazer push." "error"
fi

if [ $WARNINGS -gt 0 ]; then
    print_status "⚠️ $WARNINGS aviso(s)" "warning"
fi

# Dicas para correção
if [ $ERRORS -gt 0 ]; then
    echo ""
    print_status "💡 Dicas para corrigir:" "info"
    echo "   - Execute 'pytest tests/' para ver falhas em testes"
    echo "   - Execute 'python build_exe.py' para testar o build"
    echo "   - Resolva conflitos de merge se houver"
    echo "   - Certifique-se de que todos os commits foram feitos"
fi

# Sair com código de erro se houver problemas críticos
if [ $ERRORS -gt 0 ]; then
    exit 1
fi

exit 0
    local type="${2:-info}"
    
    case $type in
        "success")
            echo -e "✅ ${GREEN}${message}${NC}"
            ;;
        "error")
            echo -e "❌ ${RED}${message}${NC}"
            ;;
        "warning")
            echo -e "⚠️ ${YELLOW}${message}${NC}"
            ;;
        "info")
            echo -e "ℹ️ ${BLUE}${message}${NC}"
            ;;
        *)
            echo -e "${message}"
            ;;
    esac
}

# Header
echo ""
print_status "🚀 Executando verificações pre-push..." "info"

# Verificar branch atual
BRANCH=$(git branch --show-current)
print_status "Branch atual: $BRANCH" "info"

# 1. Executar suite de testes
print_status "Executando suite completa de testes..." "info"
if python -m pytest tests/ -v; then
    print_status "Todos os testes passaram" "success"
else
    print_status "Falhas em testes detectadas!" "error"
    ERRORS=$((ERRORS + 1))
fi

# 2. Verificar cobertura de testes
print_status "Verificando cobertura de testes..." "info"
COVERAGE=$(python -m pytest --cov=. --cov-report=term-missing tests/ 2>/dev/null | grep "TOTAL" | awk '{print $4}' | sed 's/%//')

if [ ! -z "$COVERAGE" ]; then
    if [ "$COVERAGE" -lt 70 ]; then
        print_status "Cobertura baixa: ${COVERAGE}% (mínimo 70%)" "warning"
        WARNINGS=$((WARNINGS + 1))
    else
        print_status "Cobertura de testes: ${COVERAGE}%" "success"
    fi
else
    print_status "Não foi possível calcular cobertura" "warning"
    WARNINGS=$((WARNINGS + 1))
fi

# 3. Testar build (informativo apenas)
print_status "Testando geração do executável..." "info"
python build_exe.py > build_log.txt 2>&1
if [ $? -eq 0 ]; then
    print_status "Executável gerado com sucesso" "success"
    rm -f build_log.txt
else
    print_status "Falha na geração do executável (não bloqueia push)" "warning"
    if [ -f "build_log.txt" ]; then
        echo "Últimas linhas do erro:"
        tail -5 build_log.txt
    fi
    WARNINGS=$((WARNINGS + 1))
fi

# 4. Verificar TODOs e FIXMEs
print_status "Verificando TODOs e FIXMEs..." "info"
if git diff --cached --name-only | grep -E '\.(py|yml|yaml|json)$' | xargs grep -n "TODO\|FIXME" 2>/dev/null; then
    print_status "TODOs/FIXMEs encontrados - considere resolver" "warning"
    WARNINGS=$((WARNINGS + 1))
else
    print_status "Nenhum TODO/FIXME encontrado" "success"
fi

# 5. Verificar se documentação está atualizada
print_status "Verificando documentação..." "info"
if [ -f "README.md" ]; then
    README_AGE=$(git log -1 --format=%ct README.md 2>/dev/null || echo 0)
    CODE_AGE=$(git log -1 --format=%ct *.py 2>/dev/null || echo 0)
    
    if [ $CODE_AGE -gt $README_AGE ]; then
        print_status "README pode estar desatualizado" "warning"
        WARNINGS=$((WARNINGS + 1))
    else
        print_status "Documentação atualizada" "success"
    fi
fi

# 6. Verificações específicas por branch
case $BRANCH in
    "main")
        print_status "Verificações para branch main..." "info"
        
        # Verificar se versão foi atualizada (se há tag)
        LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null)
        if [ ! -z "$LAST_TAG" ]; then
            # Verificar se há commits desde a última tag
            COMMITS_SINCE_TAG=$(git rev-list ${LAST_TAG}..HEAD --count)
            if [ $COMMITS_SINCE_TAG -gt 0 ]; then
                print_status "$COMMITS_SINCE_TAG commits desde $LAST_TAG - considere criar nova tag" "warning"
                WARNINGS=$((WARNINGS + 1))
            fi
        fi
        ;;
        
    "develop")
        print_status "Verificações para branch develop..." "info"
        # Verificações específicas para develop
        ;;
        
    *)
        print_status "Branch feature detectada" "info"
        ;;
esac

# 7. Verificar se há conflitos de merge pendentes
print_status "Verificando conflitos de merge..." "info"
if git ls-files -u | grep -q .; then
    print_status "Conflitos de merge detectados!" "error"
    ERRORS=$((ERRORS + 1))
elif grep -rE "^<<<<<<< |^>>>>>>> |^=======$" --include="*.py" --include="*.yml" --include="*.yaml" --include="*.json" --exclude-dir=".venv" --exclude-dir="htmlcov" --exclude-dir="__pycache__" . 2>/dev/null; then
    print_status "Possíveis marcadores de conflito encontrados!" "error" 
    ERRORS=$((ERRORS + 1))
else
    print_status "Nenhum conflito detectado" "success"
fi

# 8. Verificar qualidade dos commits
print_status "Verificando qualidade dos commits..." "info"
LAST_COMMIT_MSG=$(git log -1 --pretty=%B)
if echo "$LAST_COMMIT_MSG" | grep -qE "^(feat|fix|docs|style|refactor|test|chore)(\(.+\))?: .+"; then
    print_status "Commit segue Conventional Commits" "success"
else
    print_status "Commit não segue Conventional Commits (recomendado)" "warning"
    WARNINGS=$((WARNINGS + 1))
fi

# Resumo final
echo ""
print_status "📊 Resumo das verificações:" "info"
if [ $ERRORS -gt 0 ]; then
    print_status "❌ Erros: $ERRORS" "error"
fi
if [ $WARNINGS -gt 0 ]; then
    print_status "⚠️ Avisos: $WARNINGS" "warning"
fi

# Decisão final
if [ $ERRORS -eq 0 ]; then
    print_status "🎉 Push autorizado!" "success"
    
    if [ $WARNINGS -gt 0 ]; then
        print_status "⚠️ $WARNINGS aviso(s) encontrado(s), mas não são bloqueantes." "warning"
    fi
    
    exit 0
else
    print_status "⛔ $ERRORS erro(s) encontrado(s). Corrija antes de fazer push." "error"
    echo ""
    echo "💡 Dicas para corrigir:"
    echo "   - Execute 'pytest tests/' para ver falhas em testes"
    echo "   - Execute 'python build_exe.py' para testar o build"
    echo "   - Resolva conflitos de merge se houver"
    echo "   - Certifique-se de que todos os commits foram feitos"
    
    exit 1
fi
